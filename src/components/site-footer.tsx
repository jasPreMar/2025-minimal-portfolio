"use client";

import { motion } from "motion/react";

export function SiteFooter() {
    return (
        <footer className="mt-40">
            <p className="text-secondary text-base">
                Made with ❤️ by Jason Marsh in Cursor with help from Claude
            </p>
        </footer>
    );
}
